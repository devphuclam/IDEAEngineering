# Q-15 Phase 3 Decision-Grade Qualification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Harden both Q-15 candidates, execute the decision-grade evidence available on the current machine, and record an honest Option A / Option B / no-winner result.

**Architecture:** Preserve the shared API, Workspace, fixture and protocol. Replace Option B's unsafe caller-owned Win32 asynchronous state with a typed native operation object; add observable keyboard/Web delivery behavior; then run same-machine Release comparisons and retain immutable successor evidence.

**Tech Stack:** C++/Win32 named pipes, Dart FFI, Flutter Web/Windows 3.47.4, React/TypeScript/WebView2/WPF, .NET 10 harnesses, Playwright/Edge, PowerShell evidence runners.

**Spec:** `docs/superpowers/specs/2026-09-15-q15-phase3-design.md`

## Global Constraints

- Baseline is `8bc0f955247a5c635498dbd76dce9527a2070b13`; preserve unrelated `.gitattributes`, `RUN_VERIFIERS.bat` and local `TestResults` changes.
- No Product feature or Product Scope, FTR, REQ, architecture, Spec, Tech, DDM semantic, PG3 or PG4 change.
- Do not run repository documentation/diagram verifier scripts; run only focused Q-15 build/test/evidence commands.
- Do not install SDKs, packages or tools; missing environment capability is `BLOCKED`.
- Phase 1/2 evidence is immutable; Phase 3 uses `IE-VEV-TECH-Q15-003` and a new run ID.
- A planned test is never `PASS`; raw runner output must support each summary.

---

### Task 1: First-party semantics and native I/O state machine

**Files:**
- Create: `qualification/q15/option-b/flutter/windows/runner/q15_io_shim_tests.cpp`
- Modify: `qualification/q15/option-b/flutter/windows/runner/q15_io_shim.h`
- Modify: `qualification/q15/option-b/flutter/windows/runner/q15_io_shim.cpp`
- Modify: `qualification/q15/option-b/flutter/windows/runner/CMakeLists.txt`
- Modify: `qualification/q15/option-b/flutter/lib/workspace/workspace_client_windows.dart`
- Modify: `qualification/q15/option-b/flutter/integration_test/ffi_fault_integration_test.dart`
- Modify: `qualification/q15/scripts/Invoke-Q15FfiFaultIntegration.ps1`
- Research input: `docs/research/2026-09-15-q15-phase3-win32-and-flutter-offline-runtime-evidence.md`

**Interfaces:**
- Consumes: Win32 named-pipe handle and caller data; current typed Workspace protocol.
- Produces: typed connect/start/wait/cancel/release/detach ABI and diagnostic snapshot; Dart returns only terminal byte counts or classified errors.

- [x] Add native tests for immediate read/write, pending read/write, immediate peer-close failure, cancel race, `ERROR_OPERATION_ABORTED`, detached cleanup and repeated cycles.
- [x] Add the native test target and run the Release build to observe RED because the typed ABI does not exist.
- [x] Implement the smallest typed ABI and native-owned operation lifetime that satisfies those tests.
- [x] Replace separate Dart Win32/`GetLastError` calls and unconditional finish logic with the typed ABI.
- [ ] Expand the real Flutter fault runner while retaining all ten Phase 2 cases; record nondeterministic branches explicitly.
- [ ] Run native tests, focused Flutter Windows integration and 100+ fault/reconnect cycles; retain raw results and process resource snapshots.
- [ ] Commit the independently reviewable native correctness slice.

### Task 2: Grid keyboard and visibility contract

**Files:**
- Modify: `qualification/q15/option-b/flutter/test/widget_test.dart`
- Modify: `qualification/q15/option-b/flutter/lib/ui/paged_grid.dart`
- Modify: `qualification/q15/option-a/web/tests-e2e/phase2-qualification.spec.ts`
- Modify: `qualification/q15/option-a/web/src/VirtualGrid.tsx` only if RED exposes a real Option A defect.

**Interfaces:**
- Consumes: 100,000-row paged fixture, `onNeedRow`, `onSelect`, `onOpen` and frozen keyboard mapping.
- Produces: observable selection + viewport + focus behavior for Home, End, PageUp, PageDown, arrows, Enter, Space, F2 and Shift+F10.

- [x] Write Flutter and Playwright tests asserting exact selected row, visible row, requested page and focus for the frozen grid keys.
- [x] Run each focused suite and observe RED for missing End visibility/PageUp/PageDown or context-menu focus behavior.
- [x] Add one vertical controller/index-scroll implementation without eagerly rendering rows.
- [x] Run both focused suites to GREEN; the checkpoint output is summarized in `qualification/q15/HANDOFF-PHASE3.md`, while immutable VEV raw capture remains pending.
- [ ] Commit the independently reviewable keyboard slice.

### Task 3: Controlled-origin Flutter Web runtime and fonts

**Files:**
- Create: `qualification/q15/option-b/flutter/web/flutter_bootstrap.js`
- Create: legally redistributable font assets and their license files under `qualification/q15/option-b/flutter/assets/fonts/`
- Modify: `qualification/q15/option-b/flutter/web/index.html`
- Modify: `qualification/q15/option-b/flutter/pubspec.yaml`
- Modify: `qualification/q15/option-b/flutter/lib/ui/q15_app.dart`
- Modify: `qualification/q15/shared/browser-tests/option-b-web.spec.ts`
- Modify: `qualification/q15/scripts/Invoke-Q15OptionBWebIntegration.ps1`

**Interfaces:**
- Consumes: Flutter-generated local `canvaskit/` artifacts and declared application fonts.
- Produces: JS/Wasm Release outputs whose required UI runtime requests remain on the controlled origin, with EN/VI/JA rendering evidence.

- [ ] Write a browser test that blocklists `gstatic.com`, fails on any required public request, and checks login, EN, VI, JA and critical flow.
- [ ] Run against the current build and observe RED from renderer/font public-origin requests.
- [ ] Configure `canvasKitBaseUrl` and `fontFallbackBaseUrl` to controlled-origin paths and bundle only license-compatible fonts.
- [ ] Build/run JS and Wasm Release outputs with public origins blocked; retain request/response and transfer-size evidence.
- [ ] Record font family, source, license, fallback policy and payload cost.
- [ ] Commit the independently reviewable Web self-hosting slice.

### Task 4: Release installed flow and counterbalanced resource capture

**Files:**
- Modify: `qualification/q15/option-b/flutter/lib/qualification_smoke_io.dart`
- Modify: `qualification/q15/option-a/desktop/Idea.Q15.OptionA.Desktop/InstalledFlowScript.cs`
- Modify: `qualification/q15/option-a/desktop/Idea.Q15.OptionA.Desktop/MainWindow.xaml.cs`
- Create: `qualification/q15/scripts/Invoke-Q15Phase3ReleaseComparison.ps1`
- Modify: `qualification/q15/shared/metrics/measurement-definition.md`

**Interfaces:**
- Consumes: actual A/B Release binaries, common API/Workspace/fixture/account and fixed AB/BA order.
- Produces: `LOGIN_UI_READY`, `Q15_ENGINEERING_READY`, full-flow result and process-tree CPU/memory/handle/thread samples with build hashes and run order.

- [ ] Write runner preflight assertions that reject Debug binaries, unequal fixture/config or non-counterbalanced order.
- [ ] Run the preflight against an intentionally invalid input and observe RED.
- [ ] Add common engineering-readiness markers and the Release comparison runner.
- [ ] Execute A/B Release flows in AB/BA order and retain every raw sample; do not infer percentiles from an undersized set.
- [ ] Commit measurement code separately from generated evidence.

### Task 5: Browser parity and environment-bound checks

**Files:**
- Modify: candidate Playwright suites under `qualification/q15/option-a/web/tests-e2e/` and `qualification/q15/shared/browser-tests/`
- Create: bounded evidence scripts only where an available OS facility can produce real results.

**Interfaces:**
- Consumes: three production Web surfaces and the current Windows account/environment.
- Produces: scenario-level PASS/FAIL/BLOCKED/NOT-RUN/NOT-APPLICABLE results for refresh, back, forward, clipboard, cold deep link, outage and multiple tabs, plus honest IME/accessibility/security disposition.

- [ ] Add behavior tests for each applicable browser case, run them against React, Flutter JS and Flutter Wasm, and retain raw Playwright output.
- [ ] Inspect available built-in IME, Narrator/high-contrast and Windows account/elevation capabilities without installing or changing unauthorized system policy.
- [ ] Run only genuinely available bounded tests; preserve Unicode rendering separately from IME and semantics/ARIA separately from assistive-technology results.
- [ ] Commit the browser/environment qualification code.

### Task 6: Immutable Phase 3 package and engineering disposition

**Files:**
- Create: `docs/product/instances/idea-engineering/evidence/IE-VEV-TECH-Q15-003/<run-id>/...`
- Create: `docs/product/instances/idea-engineering/registers/VEV-2026-09-15-q15-client-ui-architecture-qualification-phase3.md`
- Modify: `docs/product/instances/idea-engineering/README.md`
- Modify Matrix/TECH-001 only if the evidence materially supports a recommendation-state change.

**Interfaces:**
- Consumes: exact implementation SHA, raw outputs, environment snapshot, build hashes and predecessor Q15-002.
- Produces: manifest-backed VEV-003 package and one explicit technical-winner/confidence result; Product Decision Authority remains `NOT-RUN`.

- [ ] Copy raw outputs losslessly, create normalized result/resource/browser/font/maintenance summaries, and link every summary to raw evidence.
- [ ] Record Phase 2 errata for Home/End, IME, accessibility, direct-FFI/shim, Release and browser-parity wording without rewriting predecessor files.
- [ ] Compare Web, Windows-native, reuse, maintenance and correctness/security without numeric scoring.
- [ ] Assign `OPTION A`, `OPTION B` or `NO WINNER` only from the retained mandatory evidence.
- [ ] Generate and independently recheck the SHA-256 manifest; record repository verifier scripts as `NOT-RUN`.
- [ ] Review the final diff for scope and evidence overclaims, commit implementation and evidence separately, and push only when explicitly instructed.
