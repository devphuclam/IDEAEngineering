# P03/T016 Decision Review

| Field | Value |
|---|---|
| Evidence ID / class | `P03-T016-DECISIONS-20260925` / attributable PH0 documentary review |
| Version / status | `0.1` / Reviewed for the bounded PH1 readiness scope |
| Increment / work package | `IE-INC-READY-001` / P03, T016 |
| Review date | 2026-09-25 (Asia/Ho_Chi_Minh) |
| Reviewer | Project user acting as Project Reviewer and PG4 Gate Authority where the decision concerns PH1 readiness |
| Method | Guided review of D0–D5 in the readiness register against the current source set, prior P04–P06 evidence and the F01–F05 scope. The user selected each recommended disposition; the boss's PG2/PG3 decision is recorded separately. |
| Result | `PASS` for the P03 decision/dependency account, not for application behavior or PG4 |
| Source baseline | Git `4920f6896cf88735a7174d36246945e5209ad3ca` for existing product/PH0 sources; current decision report and register edits require T023 freeze before PG4 |
| Product normativity | No new Feature/Requirement/Tech selection; binds only the stated PH1 readiness and later reopen conditions |
| Access / retention | `INTERNAL`; retain with P03 and P07 evidence |

## Decisions and gate effects

| ID | Attributable disposition | PG4 effect | Later trigger / required proof |
|---|---|---|---|
| D0 | The project user reports the boss approved the current Vault-transfer design on 2026-09-25. PH1 implements one Gateway/Vault endpoint; multi-location design is approved but its implementation is deferred. Exact content and the ADR-0009 exception are in [IE-CHG-PDA-APPROVAL-004](../../../docs/product/instances/idea-engineering/registers/CHG-2026-09-25-pg2-pg3-approval.md). | `NONE` for bounded PH1; not approval of PG4 | `DEFERRED_SCOPE` implementation of multiple Vaults, replication, repair and failover; reopen before W06/operational durability claim. |
| D1-A | The user accepted a single-endpoint Gateway/Adapter boundary and a concrete F05 verification plan before PG4. The current developer owns it. Exact runtime, provider, package license and running endpoint are not selected or tested yet. | `NONE` for PG4 design/readiness; do not call the Gateway runtime qualified | `BLOCKS_LATER_MILESTONE` F05/MS1 until exact runtime/Adapter intake, private staging, Grant/Receipt, digest, wrong-scope/expired-grant and direct-byte-path tests pass. |
| D1-B | Format Worker boundary remains selected for Core v0; no format job is in F01–F05. Its exact runtime/toolchain remains `NOT-RUN`. | `DEFERRED_SCOPE` for PH1 | Reopen before first CAD/Office conversion, preview, extraction or representation job. |
| D2 | The user accepted the existing Ubuntu/PostgreSQL/one-Vault development host for F01–F05. Native IDEA accounts, application connectivity and Gateway endpoint are PH1 deliverables, not preexisting PG4 proof. | `NONE` for PG4 environment allocation, based on scoped P04 PASS and current SSH recheck | `BLOCKS_LATER_MILESTONE` F03/F05 acceptance until accounts and endpoints work; review shared/company deployment separately. |
| D3 | The boss-approved P06 Security/Verification review scope and the guided documentary PASS remain as previously recorded. | `NONE` for current documentary PG4 preparation | `BLOCKS_LATER_MILESTONE` for matching runtime/security acceptance; reopen for production recovery, backup, failover or SLA claims. |
| D4 | The user, as data custodian and delegated PG4 Gate Authority, accepted the P05 deterministic 1 KiB and 64 MiB synthetic fixtures for F05 smoke checks only. | `NONE` for bounded PH1 PG4 data readiness | `DEFERRED_SCOPE` multi-GB, throughput, concurrency and resource-limit qualification; no performance claim. Review/delete fixture files by 2027-01-31. |
| D5 | The user accepted existing PostgreSQL/Temurin/Node.js internal-development intake. Any new package, asset or service must have exact source/version/license checked **before first use**; later application lockfiles cannot be checked before F01 creates source. | `NONE` for current PH1 PG4 source-intake plan; no commercial license claim | `BLOCKS_LATER_MILESTONE` for use of each unqualified future dependency; commercial redistribution requires a separate review. |

The D1-A verification plan follows the current F05 boundary: the Server authorizes and issues a
scoped Grant; the Client sends bytes directly to one Gateway near the Vault; the Gateway verifies
size/digest and returns a correlated Receipt; the Server records only the verified outcome and does
not proxy complete bytes. F05 must test allowed and denied paths with the P05 fixtures. This plan
does not select a Gateway runtime or pretend that the application already exists.

## Current environment recheck

On 2026-09-25 the assistant used the existing IDEA-specific SSH key in batch mode to access
`phuclam@192.168.137.33`. Read-only commands returned host `ideaddmserver`, `wlp2s0 UP
192.168.137.33/24`, `postgresql active`, and `/srv/idea/artifacts` mounted ext4
`rw,nosuid,nodev,relatime`. The first SSH attempt without specifying the IDEA key was refused;
the explicit-key attempt succeeded. No password or private key content was read or recorded.
This recheck proves host access and service/mount state only; it does not test IDEA endpoints,
database writes, Gateway I/O or backup/restore.

## Remaining work before PG4

P03 is complete as a **decision account**. It does not close T023–T025, T031 or the PG4 gate.
The Project Reviewer chose to leave ADR-0009 Proposed because its graphical-workflow example
conflicts with the approved Core v0 Feature/Spec; PH1 F01–F05 does not implement workflow design.
The reported PG3 approval is therefore usable only for PH1 until that conflict is resolved.
Gateway runtime selection, new-dependency intake and application checks are due at their named
implementation checkpoints, not silently treated as already passed.
